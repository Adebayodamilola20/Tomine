/**
 * The single ordering line. It appears in the navbar, the hero, the floating
 * button and every service page, so it lives here rather than being retyped.
 */
export const WHATSAPP_NUMBER = '2349036152411';
export const WHATSAPP_ORDER = `https://wa.me/${WHATSAPP_NUMBER}`;
